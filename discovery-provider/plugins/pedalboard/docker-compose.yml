version: "3"

services:
  template:
    container_name: template
    build:
      context: .
      dockerfile: ./apps/app-template/Dockerfile
    restart: always
    ports:
      - 3000:3000
    networks:
      - app_network

networks:
  app_network:
    external: true
