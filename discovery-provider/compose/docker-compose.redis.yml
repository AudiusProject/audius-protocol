# Compose file to run the discovery redis container
version: '3'
services:
  redis-server:
    image: redis:5.0.4
    command: redis-server --save '' 
    ports:
      - '${audius_redis_port}:6379'
    networks:
      - audius_dev

networks:
  audius_dev:
    external: true