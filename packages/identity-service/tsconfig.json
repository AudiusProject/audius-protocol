// This is used by `ts language service` and testing, so it needs to include the test folder
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./build_test",
    "types": ["node", "mocha"]
  },
  "include": [
    "src",
    "test",
    "sequelize"
  ],
  "exclude": [
    "node_modules",
    "build"
  ]
}
