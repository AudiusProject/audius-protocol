import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { HoverCard } from './HoverCard'
import { HoverCardHeader } from './HoverCardHeader'

<Meta title='Components/HoverCard' component={HoverCard} />

# HoverCard

HoverCard is a component that displays content in a hover card/tooltip with a Paper container
when the user hovers over the component's children.

The hover card appears to the right of the child element by default and disappears when the user's mouse leaves either the trigger or the card.

## Basic Usage

<Canvas>
  <Story id='components-hovercard--basic' />
</Canvas>

## With Custom Anchor and Transform Origin

You can customize the position of the hover card by specifying the `anchorOrigin` and `transformOrigin` props.

<Canvas>
  <Story id='components-hovercard--custom-position' />
</Canvas>

## With Header

The HoverCard is often used with a HoverCardHeader component that provides a title and icon.

<Canvas>
  <Story id='components-hovercard--with-header' />
</Canvas>

## API Reference

### HoverCard Props

<ArgsTable of={HoverCard} />

### HoverCardHeader Props

<ArgsTable of={HoverCardHeader} />
