import {
  Meta,
  Title,
  Description,
  Primary,
  Controls,
  Canvas
} from '@storybook/blocks'

import paperExampleGood from 'storybook/assets/paperExampleGood.png'
import {
  ComponentRules,
  Heading,
  RelatedComponents
} from 'storybook/components'
import { Box } from '~harmony/components'

import * as PaperStories from './Paper.stories'

<Meta of={PaperStories} />

<Title />

- [Overview](#overview)
- [Props](#props)
- [Use cases and examples](#use-cases-and-examples)
- [Do's and dont's](#dos-and-donts)

<Heading>
  ## Overview
  <Description />
</Heading>
<Primary />

## Props

_Note: Paper also accepts layout props from [Flex](?path=/docs/components-layout-flex--documentation)_

<Controls />

## Configuration Options

### Interactive

<Canvas of={PaperStories.Interactive} />

## Use cases and examples

- The paper component has pre-set properties for color, corner radius, shadow, border color, and spacing.
- Use the paper component to quickly create containers and eliminate guesswork.
- Ensure consistency by using the paper component to create containers that follow Harmony’s foundational properties.
- Use the paper components onClick prop to apply interactive states to the component (such as hover and press)

## Do's and don'ts

<ComponentRules
  rules={[
    {
      positive: {
        description:
          'Use paper to create spin-off component in your local library.',
        component: <img src={paperExampleGood} />
      },
      negative: {
        description:
          'Don’t apply properties not built into the Paper component.',
        component: (
          <Box
            w={347}
            h={93}
            pv={17}
            ph={18}
            style={{
              border: '5px solid #6A677A',
              borderRadius: '20px',
              background: '#F5F5F5'
            }}
          />
        )
      }
    }
  ]}
/>

## Related components

<RelatedComponents componentNames={['Divider', 'Flex', 'Box']} />
