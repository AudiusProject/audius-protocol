import { Meta, Title, Unstyled } from '@storybook/blocks'

import { TextDropShadow } from 'storybook/assets/TextDropShadow'
import shadowBackground from 'storybook/assets/shadowBackground.jpeg'
import { RelatedFoundations, Heading } from 'storybook/components'
import { Text, Flex, Paper } from '~harmony/components'

<Meta title='Foundations/Shadow' />

<Title>Shadow</Title>

- [Overview](#overview)
- [Shadow levels](#shadow-levels)
- [Usage and examples](#usage-and-examples)

<Heading>
## Overview

Shadows offer elevation by distinguishing surface distances along the z-axis. Use Harmony's pre-defined shadow styles for consistent visual effects.

</Heading>

export const centerProps = {
  alignItems: 'center',
  justifyContent: 'center'
}

<Unstyled>
  <Flex gap='3xl'>
    <Paper shadow='far' h={200} w={200} borderRadius='xl' {...centerProps}>
      <Paper shadow='mid' h={140} w={140} borderRadius='m' {...centerProps}>
        <Paper
          shadow='near'
          borderRadius='s'
          h={80}
          w={80}
          {...centerProps}
        ></Paper>
      </Paper>
    </Paper>
    <TextDropShadow />
    <Paper
      shadow='special'
      h={200}
      w={200}
      borderRadius='xl'
      css={{
        background: `url(${shadowBackground}), lightgray 50%`,
        backgroundSize: 'cover'
      }}
    />
  </Flex>
</Unstyled>

## Shadow lewels

export function ShadowLevel(props) {
  const { shadow, ...other } = props
  return (
    <Flex direction='column' gap='l' alignItems='center'>
      <Paper h={80} w={80} shadow={shadow} {...other} />
      <Text>{shadow}</Text>
    </Flex>
  )
}

<Unstyled>
  <Flex justifyContent='space-between'>
    <ShadowLevel shadow='near' />
    <ShadowLevel shadow='mid' />
    <ShadowLevel shadow='midInverted' />
    <ShadowLevel shadow='far' />
    <ShadowLevel shadow='emphasis' />
    <ShadowLevel
      shadow='special'
      css={{
        background: `url(${shadowBackground}), lightgray 50%`,
        backgroundSize: 'cover'
      }}
    />
  </Flex>
</Unstyled>

| Name                | Description                                                                                       |
| ------------------- | ------------------------------------------------------------------------------------------------- |
| shadow-near         | Optimal for onPress states on cards.                                                              |
| shadow-mid          | Used for smaller card elements and the default state for card elements with a hover interaction.  |
| shadow-mid-inverted | This is the same as mid but with the values offset the other direction.                           |
| shadow-far          | Used for hover state for card elements, larger cards/sections without hover states, and modals    |
| shadow-emphasis     | Used for separating elements from their background                                                |
| shadow-special      | Shadow derived from the dominate color of the artwork it’s surrounding. Used for playing artwork. |

## Usage and examples

- Shadows should be applied consistently throughout your work.
- Do not apply different shadow variants to the same components.
- Do not elevate near shadows above far shadows or vice versa.
- Use shadow to indicate interaction

<Heading>
### Interactions Examples

Use shadows to indicate a card appearing closer or farther on press and hover.

</Heading>

<Unstyled>
  <Paper h={200} flex={1} alignItems='center' justifyContent='center'>
    <Text variant='label' size='xl'>
      TODO
    </Text>
  </Paper>
</Unstyled>

Use shadows to indicate a popover is above the content.

<Unstyled>
  <Paper h={200} flex={1} alignItems='center' justifyContent='center'>
    <Text variant='label' size='xl'>
      TODO
    </Text>
  </Paper>
</Unstyled>

Use shadow-far to indicate an elements that is above the page, preventing the interaction with the rest of the elements. (ex: modal)

<Unstyled>
  <Paper h={200} flex={1} alignItems='center' justifyContent='center'>
    <Text variant='label' size='xl'>
      TODO
    </Text>
  </Paper>
</Unstyled>

## Related Foundations

<RelatedFoundations foundationNames={['Color', 'Typography', 'CornerRadius']} />
