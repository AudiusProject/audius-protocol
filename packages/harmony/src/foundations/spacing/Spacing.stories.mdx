import { useTheme } from '@emotion/react'
import { Meta, Title, Unstyled } from '@storybook/blocks'

import spacingExample from 'storybook/assets/spacingExample.png'
import { Heading, RelatedFoundations } from 'storybook/components'
import { Paper, Flex, Text } from '~harmony/components'

<Meta title='Foundations/Spacing' />

<Title>Spacing</Title>

- [Overview](#overview)
- [Spacing sizes](#spacing-sizes)
- [Containers](#containers)
- [Usage and examples](#usage-and-examples)

<Heading>
## Overview

Consistent and scalable spacing helps you eliminate guesswork because you're designing with a limited set of options.

</Heading>

<Heading>
## Spacing sizes

Using the right sizes will create consistency and hierarchy in user interface.

</Heading>

export function SpacingExample(props) {
  const { spacing } = props
  const theme = useTheme()
  const spacingValue = theme.spacing[spacing]
  return (
    <Flex gap='xl' direction='column' alignItems='center'>
      <Flex
        alignItems='center'
        justifyContent='center'
        h={spacingValue}
        w={64}
        css={{ background: 'rgba(255, 0, 0, 0.10)' }}
      >
        <Text css={{ color: '#F00', fontSize: Math.min(spacingValue, 10) }}>
          {spacingValue}
        </Text>
      </Flex>
      <Text>{spacing}</Text>
    </Flex>
  )
}

<Unstyled>
  <Paper p='xl' justifyContent='space-between' alignItems='flex-end'>
    {['xs', 's', 'm', 'l', 'xl', '2xl', '3xl', '4xl'].map((spacing) => (
      <SpacingExample key={spacing} spacing={spacing} />
    ))}
  </Paper>
</Unstyled>

<div css={{table: {width: '100%'}}}>

| Name | Size |
| ---- | ---- |
| XXS  | 2px  |
| XS   | 4px  |
| S    | 8px  |
| M    | 12px |
| L    | 16px |
| XL   | 24px |
| 2XL  | 32px |
| 3XL  | 48px |
| 4XL  | 64px |

</div>

## Usage and examples

- Stay consistent with paddings and sizes across different UI components.
- Align all the elements to the same spacing within the components or a page.
- Elements should be grouped by likeness, with the most similar elements closer together and unlike elements farther apart.

<img src={spacingExample} />

## Related Foundations

<RelatedFoundations foundationNames={['Color', 'Shadow', 'CornerRadius']} />
