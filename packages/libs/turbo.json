{
  "extends": ["//"],
  "pipeline": {
    "build:node": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"],
      "outputMode": "new-only"
    },
    "build:web": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"],
      "outputMode": "new-only"
    }
  }
}
