export enum SignUpStep {
  createAccount = 'create-account',
  createPassword = 'create-password',
  pickHandle = 'pick-handle',
  finishProfile = 'finish-profile',
  selectGenres = 'select-genre',
  selectArtists = 'select-artists'
}
