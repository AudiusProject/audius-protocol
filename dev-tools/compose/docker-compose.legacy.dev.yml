version: '3.9'

services:
  storage:
    image: busybox:1.31.1
    entrypoint: ["sh", "-c", "exit 0"]
    profiles:
      - noop

  core:
    image: busybox:1.31.1
    entrypoint: ["sh", "-c", "exit 0"]
    profiles:
      - noop
