version: '3.9'

services:
  ddex:
    container_name: ddex
    build:
      context: ${PROJECT_ROOT}
      dockerfile: ${PROJECT_ROOT}/packages/ddex/Dockerfile
      args:
        app_name: ddex
        TURBO_TEAM: '${TURBO_TEAM}'
        TURBO_TOKEN: '${TURBO_TOKEN}'
    profiles:
      - ddex
