version: "3.9"

services:
  protocol-dashboard:
    container_name: protocol-dashboard
    build:
      context: ${PROJECT_ROOT}/protocol-dashboard
    ports:
      - 5173:5173
    deploy:
      mode: global
