  ####################
  # monitor load tests
  ####################

  # monitor a load test container that is colocated on prometheus-grafana-metrics

  # disabled since the test was concluded successfully
  # - job_name: 'load-test-populate'
  #   scheme: 'http'
  #   metrics_path: '/metrics'
  #   static_configs:
  #     - targets: ['host.docker.internal:8000']
  #       labels:
  #         host: 'host.docker.internal'
  #         environment: 'load-test'
  #         service: 'audius'
  #         component: 'load-test'
  #         job: 'populate'
