{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**", "build/**"],
      "dependsOn": ["^build"],
      "outputMode": "new-only"
    },
    "link": {
      "outputMode": "new-only"
    },
    "start": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"],
      "outputMode": "new-only"
    },
    "lint": {
      "outputMode": "new-only",
      "dependsOn": ["^build", "^link"]
    },
    "lint:fix": {
      "outputMode": "new-only",
      "dependsOn": ["^build", "^link"]
    },
    "verify": {
      "outputMode": "new-only",
      "dependsOn": ["^build", "^link"]
    },
    "test": {
      "outputs": ["coverage/**"],
      "dependsOn": ["^build"]
    }
  }
}
