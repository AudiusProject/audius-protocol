{
  "name": "root",
  "private": true,
  "scripts": {
    "link": "lerna link",
    "postinstall": "lerna bootstrap --no-ci && lerna run build --scope=@audius/stems && npm run install-hooks",
    "install-hooks": "mookme init --only-hook --skip-types-selection"
  },
  "devDependencies": {
    "lerna": "5.0.0"
  },
  "dependencies": {
    "@escape.tech/mookme": "1.2.0"
  }
}
