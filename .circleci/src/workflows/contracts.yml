when: << pipeline.parameters.run-contracts-workflow >>
jobs:
  - test:
      name: test-contracts
      context: GCP
      instance-name: circleci-test-contracts-$CIRCLE_BUILD_NUM
      service: contracts
