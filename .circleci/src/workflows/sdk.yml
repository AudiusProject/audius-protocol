when: << pipeline.parameters.run-sdk-workflow >>
jobs:
  - test:
      name: test-audius-libs
      context: Vercel
      service: audius-libs

  - test:
      name: test-audius-sdk
      context: Vercel
      service: audius-sdk
