pragma solidity ^0.5.0;

/**
 * This solidity file exists only to import AdminUpgradeabilityProxy from the npm package @openzeppelin/upgrades.
 * This creates build artifacts for the contract and its dependencies locally, to make truffle migrations and tests easier.
 */
import "@openzeppelin/upgrades/contracts/upgradeability/AdminUpgradeabilityProxy.sol";