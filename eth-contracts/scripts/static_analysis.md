To perform static analysis on contracts, inside `eth-contracts` run:

```
> pip3 install slither-analyzer
> slither .
```

Alternatively to setup via docker: https://github.com/trailofbits/eth-security-toolbox/